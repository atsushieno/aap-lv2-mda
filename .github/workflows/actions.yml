name: build dist

on:
  push:
  pull_request:

jobs:
  build-dist:
    # note that you should usually specify versioned workflow file.
    uses: atsushieno/aap-lv2/.github/workflows/build-dist-app.yml
